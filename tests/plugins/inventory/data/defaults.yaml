---
port:
hostname:
username: root
password: docker
platform: linux
data:
    my_var: comes_from_defaults
    only_default: only_defined_in_default
connection_options:
    dummy:
        hostname: dummy_from_defaults
        port:
        username:
        password:
        platform:
        extras:
            blah: from_defaults
